import ChatWindow from "@/components/ChatWindow";

export const metadata = {
  title: "ZionX — AI Health Chat",
  description: "Chat with your AI Preventive Health Assistant",
};

export default function ChatPage() {
  return <ChatWindow />;
}
